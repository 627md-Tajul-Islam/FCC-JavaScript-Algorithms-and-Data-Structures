// Parameters are variables that act as placeholders for the values that are to be input to a function when it is called.

function functionWithArgs(one, two) {
    console.log(one + two);
  }
  
  const hello = functionWithArgs(1,2);