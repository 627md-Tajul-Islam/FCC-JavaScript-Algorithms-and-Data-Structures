// You can run the same code multiple times by using a loop.
// while loop runs while a specified condition is true and stops once that condition is no longer true.

const our = [];
let i = 1;

while (i <= 5){
our.push(i);
i++;
}

const myArray = [];
let j = 0;
while (j < 6){
    myArray.push(j);
    j++;
}
const ulta = myArray.reverse();
console.log(ulta);

/* let i = 5;
while(i >= 0) {
  myArray.push(i);
  i--; */